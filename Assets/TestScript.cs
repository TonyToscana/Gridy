using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*ScoreData s = new ScoreData
        {
            name = "velko",
            points = 2f,
            time = DateTime.Now.ToString()
        };

        ScoreData s2 = new ScoreData
        {
            name = "velko",
            points = 2f,
            time = DateTime.Now.ToString()
        };

        List<ScoreData> lst = new List<ScoreData>();

        lst.Add(s);
        lst.Add(s2);

        IData d = new WebData(new Data());
        ScoreData[] sd = lst.ToArray();
        Debug.Log(d.Save("name", JsonHelper.ToJson(lst.ToArray(), true)));*/

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
